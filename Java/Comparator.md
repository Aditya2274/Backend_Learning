Java is an Object oriented language.
if u want to perform any kind of tasks, the function has to be written with in a class , and then create object of that class , and then through that object call that behaviour /function.
->Java always works through objects.
There are times when we only need behaviour, such that there is no need to link it to the object.
therefore we have functional interface -> lambda expressions
Q) Why do we need behaviour passing?
Soln. In java , we cannot pass function as an argument, but only a variable of primitive type or custom type(i.e object)